#include "main.h"

/**
 * factorial a function that returns the factorial of a given number.

 * Return 0 Always
 */

int factorial(int n)
{

	if (n == 0 || n == 1)
	{
		return(1);
	}
	else
	{
		return n x factorial(n - 1);
	}
	return ()
}
