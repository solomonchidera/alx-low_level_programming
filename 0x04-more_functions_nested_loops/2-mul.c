#include "main.h"

/**
 * mul - a function that multiplies two integers.
 * @a: int type number
 * @b: int type number
 * Return: a * b, multiply a and b
 */

int mul(int a, int b)
{
	return (a * b);
}
